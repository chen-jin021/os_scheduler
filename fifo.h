#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "process.h"

// if instruction is fifo
void fifo_scheduling(p* processes, int n) {
    // printf("Enter FIFO %d", processes->state);
}